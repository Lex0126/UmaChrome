"""deje esto para poder visualizar la hitbox de los personajes y del obstaculo"""
"""pygame.draw.rect(pantalla, (255, 0, 0), uma.get_hitbox(), 2)  # hitbox de Uma
        for o in obstaculos:
            pygame.draw.rect(pantalla, (0, 0, 255), o.get_hitbox(), 2)  # hitbox de cada obstáculo"""
